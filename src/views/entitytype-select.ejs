<article>
  <header><h1>Select type</h1></header>
  <% for (const [key, content] of Object.entries(entityTypes)) { %>
    <a href="/entity/add/type/<%= key %>" role="button"><%= content.readableName %></a>
  <% } %>
</article>
